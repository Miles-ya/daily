document.addEventListener('DOMContentLoaded',()=>{const menu=document.querySelector('#menuButton'),sidebar=document.querySelector('.sidebar');if(menu)menu.addEventListener('click',()=>sidebar.classList.toggle('open'));document.querySelectorAll('.filters button').forEach(button=>button.addEventListener('click',()=>{document.querySelectorAll('.filters button').forEach(x=>x.classList.remove('active'));button.classList.add('active');const tag=button.textContent.trim();document.querySelectorAll('.card').forEach(card=>card.hidden=tag!=='全部'&&!card.dataset.tags.includes(tag))}));const input=document.querySelector('#searchInput'),results=document.querySelector('#searchResults');if(input)input.addEventListener('input',async()=>{const q=input.value.trim().toLowerCase();if(!q){results.classList.remove('active');results.innerHTML='';return}const response=await fetch(window.DAILY_BASE_URL+'search-index.json'),items=await response.json();results.innerHTML=items.filter(x=>(x.title+' '+x.tags.join(' ')+' '+x.summary).toLowerCase().includes(q)).slice(0,8).map(x=>`<a href="${window.DAILY_BASE_URL+x.url}">${x.title} · ${x.score}/100</a>`).join('')||'没有匹配结果';results.classList.add('active')})});
