document.addEventListener('DOMContentLoaded',()=>{const toggle=document.querySelector('.search-toggle'),search=document.querySelector('#siteSearch'),input=document.querySelector('#searchInput'),results=document.querySelector('#searchResults');if(toggle&&search){toggle.addEventListener('click',()=>{const open=search.hidden;search.hidden=!open;toggle.setAttribute('aria-expanded',String(open));if(open)input.focus()})}let index=null;if(input)input.addEventListener('input',async()=>{const q=input.value.trim().toLowerCase();if(!q){results.innerHTML='';return}if(!index){const response=await fetch(window.DAILY_BASE_URL+'search-index.json');index=await response.json()}const matches=index.filter(x=>(x.title+' '+x.tags.join(' ')+' '+x.summary).toLowerCase().includes(q)).slice(0,8);results.innerHTML=matches.map(x=>`<a href="${window.DAILY_BASE_URL+x.url}"><span>${x.title}</span><small>${x.score}</small></a>`).join('')||'<p>没有匹配记录。</p>'})});
